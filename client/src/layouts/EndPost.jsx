import React from "react";
import { Center } from "@mantine/core";

function EndPost({ content }) {
  return <Center style={{ margin: "1rem", color: "gray" }}>{content}</Center>;
}

export default EndPost;
